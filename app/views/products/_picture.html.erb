<%#  @user.picture  %>
<% image_present = product_pic.product_picture.blob.present? rescue nil %>
<% if( image_present ) %>
  <%= image_tag(product_pic.product_picture, size: size) %>
<% else %>
  <%=  image_tag(:no_product, size: size) %>
<% end %>
